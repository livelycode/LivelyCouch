var vows = require('vows'),
assert = require('assert');
var myutils = require('../lib/myutils');

vows.describe('resolve-bindings').addBatch({
  'simple match':  {
    topic: function() {

    },
    'is valid': function(err, config) {

    }
  }
}).export(module);